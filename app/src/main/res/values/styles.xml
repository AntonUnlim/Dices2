<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="KeyboardButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textSize">@dimen/keyboard_font_size</item>
        <item name="android:gravity">center</item>
        <item name="android:onClick">onKeyboardButtonClicked</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/selectors</item>
        <item name="android:clickable">true</item>
    </style>
    <style name="KeyboardButtonCancel">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textSize">@dimen/keyboard_cancel_font_size</item>
        <item name="android:gravity">center</item>
        <item name="android:onClick">onKeyboardButtonClicked</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/selectors</item>
        <item name="android:clickable">true</item>
    </style>


</resources>
